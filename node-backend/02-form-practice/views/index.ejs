<!DOCTYPE html>
<html lang="ko">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://code.jquery.com/jquery-3.7.1.min.js" integrity="sha256-/JqT3SQfawRcv/BIHPThkBvs0OEvtFFmqPF/lYI/Cxo=" crossorigin="anonymous"></script>
    <script src="https://cdn.jsdelivr.net/npm/axios/dist/axios.min.js"></script>
  </head>
  <body>
    <!-- Validation 적용하기 실습 -->
    <!-- <form action="/join" method="post" name="join">
      이름 <input type="text" name="name" required> <br />
      비밀번호 <input type="password" name="pw" minlength="8" pattern="^(?=.*[a-z])(?=.*[A-Z])(?=.*\d)(?=.*[$@$!%*?&])[A-Za-z\d$@$!%*?&]{8,}" title="소문자, 대문자, 숫자, 특수문자를 한 번 이상 사용, 8자 이상"> <br />
      전화번호 <input type="text" name="phone" required pattern="^0\d{1,2}(-|\))\d{3,4}-\d{4}$" title="010-0000-0000 형식"> <br />
      아이디 <input type="text" name="id" minlength="5" pattern="^([a-z0-9]){5,}$"> <br />
      <fieldset>
        <legend> 성별 </legend>
        <input type="radio" name="gender" value="male" required> 남
        <input type="radio" name="gender" value="female" required> 여
      </fieldset>
      <button type="submit">회원가입</button>
    </form> -->


    <!-- get, post 요청 실습 -->
    <form action="/join" method="post" name="join">
      이름 <input type="text" name="name" id="name" required />
      <fieldset>
        <legend>성별</legend>
        <input type="radio" id="male" name="gender" value="male" required /> <label for="male">남자</label>
        <input type="radio" id="female" name="gender" value="female" required /> <label for="female">여자</label>
      </fieldset>

      <fieldset>
        <legend>생년월일</legend>
        <select name="year" id="birth">
          <% for (let i = 2023; i >= 1990;  i--) { %>
          <option value="<%= i %>"><%= i %></option>
          <% } %>
        </select> 년
        <select name="month" id="birth">
          <% for (let i = 1; i <= 12;  i++) { %>
          <option value="<%= i %>"><%= i %></option>
          <% } %>
        </select> 월
        <select name="date" id="birth">
          <% for (let i = 1; i <= 31;  i++) { %>
          <option value="<%= i %>"><%= i %></option>
          <% } %>
        </select> 일
      </fieldset>

      <fieldset>
        <legend>관심사</legend>
        <input type="checkbox" name="trip" id="interest" /> <label for="trip">여행</label>
        <input type="checkbox" name="fashion" id="interest" /> <label for="fashion">패션</label>
        <input type="checkbox" name="food" id="interest" /> <label for="food">음식</label>
      </fieldset>


      <fieldset>
        <legend>선택</legend>
        좋아하는 색상 <input type="color" id="select" />
        좋아하는 숫자 <input type="number" id="select" />
      </fieldset>

      <button type="submit">회원가입</button>

    </form>
  </body>
</html>